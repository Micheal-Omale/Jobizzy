# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Stray dirs from accidental `nuxt dev <port>` runs (e.g. `3000/`)
[0-9][0-9][0-9][0-9]/

# Vercel build output (regenerated by `nuxt build`; Vercel rebuilds from source)
.vercel

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc / editor / OS
.DS_Store
Thumbs.db
.fleet
.idea
.vscode
*.local

# Claude Code config (local tooling, not app code)
.claude/
.agents/

# Design mockups / scratch HTML
designs/
context/context/designs/

# Local env files
.env
.env.*
!.env.example

# MCP config holds API keys (InsForge + PostHog) — keep out of version control
.mcp.json
